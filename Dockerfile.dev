# 개발 환경용 Dockerfile
FROM node:22-alpine

WORKDIR /app

# Yarn 설치
RUN corepack enable && corepack prepare yarn@1.22.19 --activate

# 의존성 파일 복사
COPY package.json yarn.lock ./

# 의존성 설치
RUN yarn install

# 소스 코드 복사
COPY . .

EXPOSE 3000

CMD ["yarn", "dev"]

